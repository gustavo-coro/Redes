O JOGO YATZY MULTIUSUÁRIO EM C

Como rodar:
Utilize o comando 'make' para compilar.

Em terminais separados, primeiro execute o servidor, especificando a porta, como:
'./servidor 8080'.
Depois, execute os clientes, especificando a mesma porta do servidor, como:
'./cliente 8080'.